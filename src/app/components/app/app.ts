import {defineComponent, computed, ref, Ref, watch} from "vue";
import FractionInput from "../fraction-input/fraction-input.vue";

export default defineComponent({
    name: "App",
    components: {
        FractionInput
    },
    setup() {}
})
